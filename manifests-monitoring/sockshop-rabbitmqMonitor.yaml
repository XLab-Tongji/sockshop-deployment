apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    k8s-app: sock-shop
  name: sockshop-rabbitmq
  namespace: monitoring
spec:
  endpoints:
  - interval: 30s
    port: rabbitmq-exporter
  selector:
    matchLabels:
      k8s-app: sock-shop
